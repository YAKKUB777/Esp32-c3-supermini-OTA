name: Build ESP32-C3 Gamepad

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup arduino-cli
        uses: arduino/setup-arduino-cli@v2

      - name: Configure arduino-cli
        run: |
          arduino-cli config init
          arduino-cli config set board_manager.additional_urls "https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json"
          arduino-cli core update-index

      - name: Install ESP32 platform
        run: arduino-cli core install esp32:esp32

      - name: Install ESP32AnalogRead library
        run: arduino-cli lib install "ESP32AnalogRead"

      - name: Install BleGamepad from GitHub
        run: |
          cd /tmp
          git clone https://github.com/lemmingDev/ESP32-BLE-Gamepad.git
          mkdir -p /home/runner/Arduino/libraries
          cp -r ESP32-BLE-Gamepad /home/runner/Arduino/libraries/BleGamepad

      - name: Compile sketch
        run: |
          arduino-cli compile \
            --fqbn esp32:esp32:esp32c3 \
            --build-property build.partitions=default_4MB.csv \
            --output-dir ./output \
            sketch/

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-esp32c3-gamepad
          path: ./output/
          retention-days: 30