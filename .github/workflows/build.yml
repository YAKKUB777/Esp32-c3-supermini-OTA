name: Build ESP32-C3 Random Jammer

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Arduino CLI
        uses: arduino/setup-arduino-cli@v1

      - name: Install ESP32 Platform
        run: |
          arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Install Libraries
        run: |
          # Встановлюємо офіційну бібліотеку RF24
          arduino-cli lib install "RF24"

      - name: Compile Sketch
        run: |
          mkdir -p build
          # Компіляція під ESP32-C3. Шлях до файлу: sketch/sketch.ino
          arduino-cli compile --fqbn esp32:esp32:esp32c3 --output-dir ./build ./sketch/sketch.ino

      - name: Upload Binary Artifact
        uses: actions/upload-artifact@v4
        with:
          name: esp32c3-jammer-firmware
          path: ./build/*.bin
          if-no-files-found: error
